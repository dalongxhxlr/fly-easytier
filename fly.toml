app = "fly-easytier-ipv4"
primary_region = "nrt"

[build]

[deploy]
  strategy = "immediate"

[env]

[http_service]
  internal_port = 8080
  force_https = false
  auto_start_machines = false
  auto_stop_machines = false
  min_machines_running = 0
  processes = ["app"]
